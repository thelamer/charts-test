image:
  repository: taisun/random-images
  pullPolicy: Always
  tag: kasm

securityContext:
  privileged: true
  allowPrivilegeEscalation: true
  readOnlyRootFilesystem: false
  runAsNonRoot: false

podSecurityContext:
  runAsUser: 0
  runAsGroup: 0

service:
  admin:
    enabled: true
    ports:
      admin:
        enabled: true
        port: 10630
        targetPort: 3000

persistence:
  data:
    enabled: true
    mountPath: /opt
  varrun:
    enabled: true
